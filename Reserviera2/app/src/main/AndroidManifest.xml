<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reserviera">
    <!-- Add these permission in AndroidMan=infest.xml file -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Reserviera"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Contact"></activity>
        <activity android:name=".Book" />
        <activity android:name=".Crystal" />
        <activity android:name=".Diamond" />
        <activity android:name=".Royall" />
        <activity android:name=".SkyHall" />
        <activity android:name=".about" />
        <activity android:name=".Halls" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>